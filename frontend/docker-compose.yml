version: '3.9'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: front_app
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
